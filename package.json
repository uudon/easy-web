{
  "name": "easy-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "VitePress-based bilingual docs site derived from the Easy-Vibe implementation path.",
  "scripts": {
    "dev": "vitepress dev docs --host 0.0.0.0",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs --host 0.0.0.0",
    "sync:content": "node scripts/sync-content.mjs",
    "publish": "node scripts/publish-site.mjs"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "vitepress": "^2.0.0-alpha.16",
    "vue": "^3.5.13"
  }
}
